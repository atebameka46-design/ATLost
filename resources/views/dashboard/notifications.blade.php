@extends('layouts.dashboard', ['title' => 'Notifications', 'heading' => 'Notifications'])
@section('dashboard-content')
<div class="dashboard-page-content section-page"><div class="section-page-hero"><div><p class="dashboard-eyebrow">Centre d’alertes</p><h1>Notifications</h1><p>Retrouvez les événements importants de votre compte.</p></div></div>@if(session('status'))<div class="profile-status">{{ session('status') }}</div>@endif<section class="dashboard-card content-card"><div class="content-list">@forelse ($notifications as $notification)<div class="notification-row"><a href="{{ route('notifications.show', $notification) }}" class="notification-main"><span class="notification-icon"><i class="fa-solid fa-bell"></i></span><span><strong>{{ $notification->title }}</strong><small>{{ $notification->message }} · {{ $notification->created_at->format('d/m/Y H:i') }}</small></span></a><form method="POST" action="{{ route('notifications.destroy', $notification) }}">@csrf @method('DELETE')<button class="profile-text-link" type="submit" aria-label="Supprimer la notification"><i class="fa-solid fa-trash"></i></button></form></div>@empty<div class="content-list-empty"><strong>Aucune notification</strong><small>Les nouvelles activités apparaîtront ici.</small></div>@endforelse</div></section></div>
@endsection
