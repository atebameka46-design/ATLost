<!doctype html><html lang="fr"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Documents retrouvés — ATLost</title>@vite(['resources/css/app.css','resources/js/app.js'])</head><body class="bg-page-bg text-n-900"><main class="marketplace-page"><header class="marketplace-header"><div><p class="dashboard-eyebrow">ATLost · Base publique</p><h1>Documents retrouvés</h1><p>Parcourez les documents disponibles et partagez facilement une fiche.</p></div><a href="{{ auth()->check() ? route('dashboard') : route('login') }}" class="dashboard-outline-button">{{ auth()->check() ? 'Mon espace' : 'Se connecter' }}</a></header><form class="marketplace-search" method="GET" action="{{ route('marketplace') }}"><input name="q" value="{{ $query }}" placeholder="Rechercher par nom, lieu ou type..."><button class="dashboard-green-button">Rechercher</button></form><div class="marketplace-grid">@forelse($documents as $document)<article class="marketplace-card">@if($document->photo_path)<img src="{{ asset('storage/'.$document->photo_path) }}" alt="Document retrouvé">@else<div class="marketplace-placeholder"><i class="fa-solid fa-file-lines"></i></div>@endif<div class="marketplace-card-body"><span>{{ $document->document_type }}</span><h2>{{ $document->owner_name }}</h2><p><i class="fa-solid fa-location-dot"></i> {{ $document->location }}</p><a href="{{ route('reports.show',$document) }}" class="dashboard-green-button">Voir le document →</a></div></article>@empty<div class="marketplace-empty"><strong>Aucun document disponible</strong><p>Essayez un autre nom ou lieu.</p></div>@endforelse</div>{{ $documents->links() }}</main></body></html>
